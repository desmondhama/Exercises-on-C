#include <stdio.h>

int main() {

  int asciiChar;
  int space = 32;
  printf("Enter a value between 33-106 for its ASCII value:");
  scanf(" %d", &asciiChar);

  printf("%d",space);
  printf("ASCII --> CHARACTER: %d%c--> %c\n", asciiChar, space, asciiChar);
  return 0;
}
int Vidgoplay(void) {
  int numTest;

  printf("Enter numtest: ");
  scanf("%d",&numTest);
}
